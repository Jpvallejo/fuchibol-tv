{
  "name": "argentina-tv",
  "version": "1.0.9",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug && cd ..",
    "set-app-icon": "node scripts/set-app-icon.js"
  },
  "bin": {
    "set-app-icon": "scripts/set-app-icon.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "hls.js": "^1.6.16"
  }
}
